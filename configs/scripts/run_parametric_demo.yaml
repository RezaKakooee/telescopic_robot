# Knobs for scripts/skills/run_parametric_demo.py.
# Override on the command line, for example:
#     python scripts/skills/run_parametric_demo.py config=<value>

config: "configs/rl/skill_course.yaml"

video: false

camera: "fixed_close_dual"

fps: 25

frame_every: 4

seconds_per_speed: 4.0
