# Knobs for scripts/skills/run_chimney_descend.py.
# Override on the command line, for example:
#     python scripts/skills/run_chimney_descend.py mode=<value>

mode: "servo"

v_target: -0.5

start_z: 3.5

seed: 42

video: true
