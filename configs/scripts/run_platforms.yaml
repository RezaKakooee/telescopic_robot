# Knobs for scripts/skills/run_platforms.py.
# Override on the command line, for example:
#     python scripts/skills/run_platforms.py config=<value>

config: "configs/rl/skill_course.yaml"

video: false

camera: "fixed_angle_side_close"

fps: 25

frame_every: 4

margin: 0.03
