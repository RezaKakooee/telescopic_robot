defaults:
  - maze_level3_fixed
  - _self_

# One fixed wall layout and one fixed canonical start; random valid goal per episode.
scenario:
  maze:
    random_endpoints: false
    random_goal: true
    endpoint_min_route: 7.5
    endpoint_max_route: 35.0
